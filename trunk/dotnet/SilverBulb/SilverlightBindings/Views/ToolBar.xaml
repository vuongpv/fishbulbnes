<UserControl x:Class="SilverlightBindings.Views.ToolBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer IsEnabled="{Binding ElementName=EnableControls, Path=IsChecked}">
            <StackPanel Orientation="Vertical">
                <Button Tag="ControlPanel" Click="Button_Click" Content="Controls" />
                <Button Tag="SoundPanel" Click="Button_Click" Content="Sound" />
            </StackPanel>
        </ScrollViewer>
        <CheckBox Name="EnableControls" Grid.Row="1" Content="Enable Tools"  />
    </Grid>
</UserControl>
